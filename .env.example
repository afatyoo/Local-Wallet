WEB_PORT=3000

# MySQL (change these)
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=finance_db
MYSQL_USER=finance_user
MYSQL_PASSWORD=change_me_password

# Generate two different random values:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change_me_session_secret
TFA_ENCRYPTION_KEY=change_me_tfa_encryption_key

# Leave empty to derive from X-Forwarded-Proto, or force true on HTTPS-only deployments
COOKIE_SECURE=
BCRYPT_ROUNDS=12
SESSION_IDLE_TIMEOUT_MINUTES=30

# API protection per client IP (15-minute windows)
GLOBAL_RATE_LIMIT_MAX=1000
AUTH_RATE_LIMIT_MAX=10

# Optional SMTP email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
APP_URL=http://localhost:3000
# Notification scan interval in milliseconds (default: 6 hours, minimum: 1 minute)
NOTIFICATION_SCAN_INTERVAL_MS=21600000
